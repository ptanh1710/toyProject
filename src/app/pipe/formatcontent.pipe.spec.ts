import { FormatcontentPipe } from './formatcontent.pipe';

describe('FormatcontentPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatcontentPipe();
    expect(pipe).toBeTruthy();
  });
});
